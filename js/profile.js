import { tg } from "./common.js";

console.log("🛠 profile.js загружен");     // <- вот эта строка

document.addEventListener("DOMContentLoaded", () => {
  tg.ready();
  // …
});
